kind = "web"
previewPath = "/"
title = "RetroVision - Retroreflectivity Intelligence Platform"
version = "1.0.0"
id = "artifacts/retro-vision"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/retro-vision run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/retro-vision", "run", "build" ]
publicDir = "artifacts/retro-vision/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
